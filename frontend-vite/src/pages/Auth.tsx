import { Box, Typography } from "@mui/material";
import type React from "react";

const Auth: React.FC = () => (
	<Box p={3}>
		<Typography variant="h4" gutterBottom>
			Perfil / Autenticação
		</Typography>
		<Typography>
			Página de perfil e autenticação. Adicione aqui os componentes e
			funcionalidades relacionados ao perfil do usuário.
		</Typography>
	</Box>
);

export default Auth;
