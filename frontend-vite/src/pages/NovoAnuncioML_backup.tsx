// Página NovoAnuncioML_backup

import { Box, Typography } from "@mui/material";
import type React from "react";

const NovoAnuncioML_backup: React.FC = () => (
	<Box p={3}>
		<Typography variant="h4" gutterBottom>
			Novo Anúncio ML (Backup)
		</Typography>
		<Typography>
			Página de backup para criação de novo anúncio no Mercado Livre. Adicione
			aqui os componentes e funcionalidades relacionados ao cadastro de anúncios
			(backup).
		</Typography>
	</Box>
);

export default NovoAnuncioML_backup;
