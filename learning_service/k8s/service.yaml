apiVersion: v1
kind: Service
metadata:
  name: learning-service-external
  labels:
    app: learning-service
spec:
  selector:
    app: learning-service
  ports:
  - name: http
    port: 8002
    targetPort: 8002
    nodePort: 30002
  type: NodePort