[tool:pytest]
minversion = 6.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --cov=app
    --cov-report=html
    --cov-report=term-missing
    --cov-fail-under=80
    --strict-markers
    --strict-config
markers =
    unit: Unit tests for pure functions
    integration: Integration tests
    models: Model validation tests
    utils: Utility function tests
    validators: Validation function tests
    parsers: Parser function tests
    errors: Error scenario tests
    simulation: Campaign simulation tests
    analytics: Analytics and calculations tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::pydantic.warnings.PydanticDeprecatedSince20